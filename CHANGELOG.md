# 변경 이력

릴리스마다 `## <버전>` 섹션을 추가하세요. `release.sh`가 해당 섹션을 읽어
Sparkle 업데이트 창과 GitHub 릴리스 노트에 그대로 보여줍니다.

## 1.0.89
- 라이브러리 창을 좁혔을 때 툴바 오른쪽에 생기는 »(더보기) 버튼을 눌러도 빈 메뉴만 뜨던 문제 수정. 이제 밀려난 편집·주석 도구, 색상·굵기, 되돌리기/복사/저장/Finder/삭제를 메뉴에서 그대로 쓸 수 있고 현재 도구에 체크 표시. 스타일 항목을 열면 툴바와 같은 색상 웰·굵기 슬라이더가 메뉴 안에 나타남

## 1.0.88
- 라이브러리 창이 열린 채 캡처 모드에 들어가면 창이 미처 사라지기 전에 화면이 찍혀 결과물에 라이브러리가 반투명하게 겹치던 문제 수정. 앱 자신의 창(라이브러리·HUD·설정)은 정지 화면과 최종 캡처에서 항상 제외
- 거의 전체 화면을 선택했을 때 선택 HUD(이미지/영상/취소)가 화면 위쪽으로 밀리면서 선택 영역의 위쪽 핸들을 덮어 크기 조절이 안 되던 문제 수정. 위아래에 자리가 없으면 HUD를 영역 안쪽 아래에 핸들과 겹치지 않게 띄움. HUD 배경을 잡고 끌면 원하는 위치로 옮길 수 있고, 옮긴 뒤 선택 영역을 조정해도 그 위치를 따라감
- 라이브러리·설정 창을 ⌘W로 닫고 ⌘M으로 최소화할 수 있도록 표준 윈도우 메뉴 추가 (GitHub 이슈 #2)
- README·설치 안내를 현재 기능(창 스냅·영상 촬영·주석 도구·언어 설정·저장 위치)에 맞게 갱신하고 로고를 새 앱 아이콘으로 교체

## 1.0.87
- 앱 아이콘 교체: 어두운 유리 질감의 뷰파인더 모양 새 아이콘 (Mac App Store 판과 동일). 업데이트 후에도 Dock·Finder에 옛 아이콘이 보이면 앱을 한 번 다시 실행하거나 Dock에서 뺐다가 다시 넣으세요
- Homebrew로 설치한 경우: 앱 안의 자동 업데이트(Sparkle)로 최신 버전을 받아도 `brew upgrade`가 같은 버전을 다시 덮어쓰지 않도록 Cask에 자동 업데이트 표시를 추가

## 1.0.86
- 영어 UI 지원: 앱 전체(메뉴·라이브러리·설정·캡처 HUD·시작 안내)가 영어로 제공되고 기본 언어가 영어로 바뀝니다. 한국어는 그대로 지원 — 업데이트 후 영어로 보이면 Settings > General > "Language · 언어"에서 한국어를 선택하세요(시작 안내 팝오버에서도 바로 선택 가능, 변경 즉시 적용)
- 라이브러리 툴바의 스타일 그룹(색상·굵기)이 캡슐 모서리에 붙어 보이던 여백을 편집/주석 그룹과 같은 밀도로 정리
- 미리보기 오른쪽 아래 배율 표시(예: 77%)를 재질 배경 알약으로 바꿔 체커보드·어두운 캡처 위에서도 잘 읽히게 개선

## 1.0.83
- 1.0.82에서 추가한 "AI 에이전트에 캡처 붙여넣기" 기능 제거. 캡처는 항상 클립보드에 복사되므로, 원하는 곳에서 직접 붙여넣는 단순한 흐름으로 되돌림 (선택 툴바의 "에이전트로" 버튼·자동 붙여넣기 설정 삭제, 접근성 권한도 창 구조 인식에만 사용)

## 1.0.82
- 활성 AI 에이전트에 캡처 붙여넣기: 터미널에서 실행 중인 Claude Code·Codex·Gemini CLI 등과 Claude·ChatGPT 앱을 자동으로 찾아, 선택 툴바의 "에이전트로"(A) 버튼 한 번으로 캡처한 이미지를 그 채팅 세션에 바로 붙여넣음. 여러 세션이 있으면 선택 메뉴가 뜨고, 캡처 직전에 보던 앱의 세션이 기본 대상
- 설정 > 일반에 "이미지 캡처 후 활성 AI 에이전트에 자동 붙여넣기" 옵션 추가(기본 꺼짐). 붙여넣기 키 전송에는 손쉬운 사용(접근성) 권한이 필요하며, 없으면 안내 후 클립보드 복사까지만 수행
- 자동 업데이트 서명 키를 교체. 1.0.81 이하에서는 자동 업데이트 설치가 실패하므로, 이 버전부터는 GitHub에서 DMG를 한 번 수동으로 내려받아 설치해 주세요 — 이후 버전부터 자동 업데이트가 다시 이어집니다

## 1.0.81
- 영역/창을 고른 뒤 뜨는 선택 HUD를 두 단 툴바로 재설계. 위 행에는 무엇을 잡았는지(창 스냅이면 앱 아이콘·이름·창 전체/본문, 영역이면 "선택 영역")와 픽셀 크기·Retina 배율(2×)이 표시되고, 핸들로 조정하면 실시간으로 따라 바뀜
- 아래 행은 이미지 캡처(⏎) · 영상 촬영(R) · 취소(Esc). 1.0.75에서 없앴던 취소 버튼을 복원하고, 단축키를 키캡 모양으로 표시. 우클릭도 취소
- HUD 버튼을 새로 그려 호버·누름 상태가 분명해지고, 녹화 표시 HUD의 버튼도 같은 모양으로 통일
- 떠 있는 HUD는 시스템 라이트/다크 모드와 무관하게 항상 어두운 표면으로 고정

## 1.0.80
- 그리기 도구가 켜져 있어도 기존 주석 위를 클릭하면 그 주석을 선택·이동(1.0.79의 "위에 새로 그리기"를 되돌림). 주석 위에 겹쳐 그리려면 ⌥(option)을 누른 채 드래그
- 앱을 한국어 지역으로 선언해 시스템이 그리는 문구(툴바 `사이드바`, 색상 패널, 열기/저장 창, 텍스트 아이콘 `가가`)가 한국어로 표시

## 1.0.79
- 선택한 주석(번호·텍스트·말풍선·화살표·사각형·원)의 색상과 굵기를 툴바에서 바로 바꿀 수 있음. 주석을 선택하면 툴바가 그 주석의 스타일을 보여 주고, 바꾼 값은 이후 새 주석에도 적용
- 선택한 주석을 방향키로 1px(⇧ 10px) 이동
- 라이브러리 툴바를 아이콘+텍스트로 고정(표시 모드 전환 메뉴 제거), 도구 아이콘을 더 알맞은 것으로 교체(번호 ①, 말풍선, 모자이크, 가로 띠 잘라내기 방향)
- 이미지 바깥 여백을 클릭해도 고른 도구가 풀리지 않음(크롭만 취소)
- 주석이 항목별로 보관되어 다른 캡처를 보고 돌아와도 그대로 편집 가능(원본 PNG는 크롭 전까지 그대로, 복사·저장 시에만 합성)
- 사각형·원·모자이크는 모서리 핸들로, 화살표는 양 끝점으로 크기·방향을 다시 조절 가능
- 그리기 도구가 켜져 있으면 기존 주석 위에서도 새로 그림. 선택·이동·편집은 선택 도구(V) 또는 ⌘-클릭
- 다시 실행(⇧⌘Z) 추가, 툴바에 버튼
- 텍스트·번호 글자에 배경 대비 헤일로를 넣어 밝은/어두운 화면 어디서나 읽힘
- 도구 단축키: V 선택 · C 크롭 · N 번호 · T 텍스트 · B 말풍선 · A 화살표 · R 사각형 · O 원 · M 모자이크

## 1.0.78
- 주석 색상·선 굵기 옵션을 라이브러리 툴바에 항상 표시하도록 복구. 굵기는 다시 1–20px 슬라이더(도구를 고르기 전에도 조정 가능)
- 캡처 오버레이의 크로스헤어와 확대경을 새 시각 언어에 맞춰 다듬음: 픽셀 정렬 선 + 밑선으로 밝은 배경에서도 선명, 커서 중심은 비우고 링으로 표시, 확대경은 12pt 모서리·공통 테두리, 중심 픽셀은 브랜드 레드, 색상·좌표는 아래 판독 알약으로

## 1.0.77
- 창 위에 커서를 올렸을 때도 확대경(루페)이 크로스헤어를 따라다니도록 복구 (1.0.75에서 숨겼던 것을 되돌림)

## 1.0.76
- 중간 잘라내기를 **가로 자르기 / 세로 자르기** 두 도구로 분리. 드래그 방향으로 띠 방향을 추측하지 않고, 고른 도구가 방향을 정한다

## 1.0.75
- 캡처 오버레이에 단계별 조작 안내 추가: 진입·선택 직후 상단에 잠깐 떠서 사라지는 힌트(드래그 영역 · 클릭 창 · ⏎ 이미지 · R 영상 · Esc 취소)
- 창 스냅 하이라이트에 앱 아이콘·이름과 구역(창 전체 / 본문) 이름표를 표시해 어디가 잡힐지 바로 알 수 있게 개선
- 창 하이라이트 색을 시스템 파랑에서 브랜드 레드로 통일하고, 창 스냅 중에는 확대경을 숨겨 화면을 조용하게 정리
- 선택 후 HUD를 두 버튼 툴바로 압축: 이미지 캡처가 기본 동작(⏎), 영상 촬영은 R, Esc·우클릭 취소. 오버레이를 클릭해 조정한 뒤에도 키보드만으로 확정 가능
- 오버레이 진입·취소 시 짧은 페이드 추가, 치수 라벨·좌표 서체를 앱의 나머지 HUD와 통일
- 라이브러리 창을 네이티브 통합 툴바 + 접히는 사이드바 구조로 재구성. 편집(선택·크롭·중간 잘라내기)과 주석 도구를 그룹으로 나누고, 색상·굵기(얇게/보통/굵게)는 주석 도구를 고를 때만 미리보기 위에 표시
- 썸네일을 오늘 / 어제 / 최근 7일 / 이전 섹션으로 나누고, 파일명 대신 "방금 전 · 14:03 · 어제" 같은 시간과 GIF/MP4 배지를 표시. 항목 수는 창 부제목으로
- 캡처가 없을 때 실제 설정된 단축키를 안내하는 빈 화면과 "지금 캡처" 버튼 추가(단축키를 바꿔도 안내가 따라옴)
- 미리보기 우하단에 배율 표시(클릭하면 창에 맞춤), 크롭 중 [취소] 버튼, 목록에서 ⌫로 휴지통 이동, 우클릭 메뉴에 복사·저장 추가
- 영상 편집기의 "GIF 30 / GIF 45 / 선택 구간 MP4 저장" 버튼을 "내보내기" 메뉴 하나로 정리하고 구간 버튼 이름을 시작점/끝점으로 명확히
- 첫 실행 안내 추가: 메뉴 막대 아이콘에서 뻗어 나오는 팝오버로 위치·단축키·화면 녹화 권한을 한 번에 안내(메뉴의 "시작 안내…"로 다시 볼 수 있음)
- 영상 촬영 중에는 메뉴 막대 아이콘이 빨간 점과 경과 시간으로 바뀌고, 메뉴에서 일시정지·중지 가능
- 메뉴의 캡처 단축키를 공백으로 흉내내던 표기 대신 네이티브 위치에 표시
- 설정 창을 일반 / 저장 / 권한 / 정보 탭으로 재구성. 권한 경고는 실제로 권한이 없을 때만 일반 탭 상단에 표시, 저장 폴더는 클릭하면 Finder가 열리는 경로 컨트롤로, 정보 탭에 버전·업데이트 확인·링크 추가
- "설정 열고 앱 종료" 대신 "설정 열고 재시작" — 권한을 켠 뒤 앱이 스스로 다시 시작
- 떠 있는 HUD(선택 툴바·녹화 표시·캡처 썸네일·토스트)의 재질·모서리·테두리를 한 표면으로 통일
- 캡처 직후 썸네일: 연속 캡처가 겹치지 않고 위로 쌓이며, 커서를 올리면 사라지지 않고, 클릭하면 라이브러리에서 열림

## 1.0.74
- 앱 전반의 시각 언어를 통일: 단일 브랜드 레드 강조색, 12pt 모서리 규칙, 시스템 재질 기반 표면(`AppAppearance`)
- 설정 창을 캡처·저장 위치·권한 섹션 카드로 재구성하고, 내용이 길어져도 창 크기를 유지하도록 스크롤 지원
- 라이브러리 툴바를 시스템 헤더 재질로 교체하고 썸네일 선택 표시·개수 표기를 다듬음
- 캡처 직후 썸네일에 "클립보드에 복사됨" 안내 추가, HUD 버튼·녹화 표시 색상을 브랜드 레드로 통일
- 앱 아이콘을 새 디자인으로 재생성

## 1.0.73
- 헤더(타이틀바·툴바·탭·주소표시줄) 위에서는 창 전체가, 본문 위에서는 본문만 스냅되도록 조정

## 1.0.72
- 창 자동 스냅을 **헤더 / 본문 두 구역**으로 단순화. 헤더(타이틀바·툴바·탭·주소표시줄·북마크바) 위에서는 헤더만, 본문 위에서는 본문만 잡힌다
- 본문 안쪽을 다시 잘라내던 "주 작업 영역" 픽셀 추정을 제거. 페이지 안의 내비게이션·사이드바 때문에 선택이 제멋대로 줄어들던 문제 해소
- 헤더 경계가 커서가 창에 들어온 위치에 따라 달라지던 문제를 수정. 경계는 창 구조로만 정하고, 한 캡처 세션 안에서는 창별로 기억한다
- 접근성 권한이 없거나 앱이 창 구조를 공개하지 않을 때는 앱별 추정값을 정지 화면의 실제 경계선(±40pt)에 맞춰 보정

## 1.0.71
- 창 전체·앱 콘텐츠·주 작업 영역의 3단계 자동 스냅을 추가. 앱 내부의 헤더·탐색 사이드바와 실제 작업 패널을 한 번 더 구분
- 웹 브라우저에 한정하지 않고, 접근성 트리와 화면의 가로·세로 경계를 함께 분석해 Finder·터미널·Electron 등 모든 앱에 같은 기준 적용
- 구조가 불분명한 화면은 주 작업 영역을 억지로 자르지 않고 기존 앱 콘텐츠 선택으로 안전하게 폴백

## 1.0.70
- 접근성 권한을 허용하면 브라우저·터미널 등 각 앱이 공개한 창 구조(`AXWebArea`/스크롤·텍스트·목록·표 영역)를 기준으로 헤더와 본문을 구분하도록 보강
- 접근성 창 목록을 제공하지 않는 Chromium/Electron 앱도 focused/main window를 프레임 검증 후 안전하게 보조 인식
- 설정에 `창 구조 인식` 권한 상태·요청·시스템 설정 이동을 추가하고, 권한이 없을 때만 기존 앱별 추정값으로 폴백

## 1.0.69
- 윈도우 자동 스냅을 헤더(타이틀바·툴바·탭) 실측 기준으로 재작성. 본문은 창에서 헤더만 빼서 잡고, 사이드바/웹뷰 추정으로 어긋나던 문제를 줄임
- 라이브러리에서 Retina 캡처를 다시 복사할 때 DPI 논리 크기를 기준으로 축소되던 문제를 수정해 원본 픽셀 해상도를 유지

## 1.0.68
- 라이브러리에 중간 잘라내기 도구 추가: 좌우 또는 상하로 드래그한 띠를 제거하고 양쪽 이미지 사이에 투명 간격을 남김
- 가로로 긴 영역 선택이 세로 절단으로 잘못 판정되던 방향 감지를 수정
- 중간 잘라내기 결과를 즉시 라이브러리 PNG와 썸네일에 반영하고 `⌘Z` 되돌리기 지원
- 영역 선택 크기 라벨을 그릴 때 폰트 예외로 앱이 안내 없이 종료되던 문제 수정

## 1.0.67
- 캡처 모드에 들어가면 그 순간의 화면을 정지시켜, 뒤에서 영상이 재생 중이어도 멈춘 화면에서 영역을 고를 수 있게 개선
- 정지 화면에서 고른 영역을 그대로 저장 — 조준한 프레임과 저장된 프레임이 달라지지 않음
- 확대경(루페)도 정지 화면에서 읽어 배경과 같은 프레임을 보여 주고, 캡처 중 화면 스트림을 켜지 않아 부하 감소
- 설정에 "캡처 모드에서 화면 정지" 항목 추가(기본 켜짐, 끄면 기존처럼 라이브 화면에서 캡처)

## 1.0.66
- 화면 녹화 권한 안내창이 다른 앱 뒤에 가려지지 않고 항상 앞에 표시되도록 수정
- 예전 저장 폴더 이관 시 PNG 외 파일(gif/영상 등)이 남아 있으면 폴더를 삭제하지 않도록 안전화
- 라이브러리 저장 경로 처리 내부 안정성 개선

## 1.0.65
- 설정 창에 화면 녹화 권한 상태를 표시하고, 시스템 설정으로 바로 이동할 수 있게 개선
- 화면 녹화 권한 허용 직후 단축키를 다시 누르지 않아도 캡처가 이어지도록 폴링 추가
- 캡처 후 라이브러리 자동 열기를 설정에서 끌 수 있게 추가
- 신규 설치의 기본 저장 폴더를 Application Support로 바꿔 바탕화면 접근 권한 요청을 줄임(기존 바탕화면 폴더는 유지)
- 루페용 화면 스트림을 축소·저fps로 바꾸고, 커서가 있는 디스플레이만 스트림해 캡처 모드 부하를 줄임
- 영역 이미지 캡처에 sourceRect 경로를 쓰고, PNG 인코딩을 메인 스레드 밖으로 옮겨 캡처 직후 멈춤을 완화

## 1.0.64
- 선택 기능인 브라우저 영역 정밀 감지를 위해 접근성 권한 창을 반복해서 띄우지 않도록 수정
- 화면 녹화 시스템 권한 창과 앱 자체 안내창이 한 번에 연달아 표시되지 않도록 수정
- 업데이트 실패 후 Sparkle 확인 주기를 복구해 다음 자동/수동 업데이트를 다시 시도할 수 있도록 개선

## 1.0.63
- 캡처 직후 클립보드에 TIFF 폴백 대신 저장본과 같은 무손실 PNG만 올리도록 변경
- 일부 앱에 바로 붙여넣을 때 Retina 캡처가 살짝 흐리게 보일 수 있던 경로 제거

## 1.0.62
- 화면 녹화 권한 안내를 보강해 권한 허용 후 앱을 다시 열도록 명확히 안내
- 기존 서명 권한 기록이 남은 사용자를 위해 권한 토글/초기화 안내 추가

## 1.0.61
- 설정 창에 `macOS 로그인 시 자동 실행` 옵션 추가
- macOS 로그인 아이템 상태를 실제 시스템 설정과 동기화해 체크박스에 반영

## 1.0.60
- 창 자동 스냅에서 전체 창이 아닌 윈도우 내부/부분 영역을 이미지 캡처할 때도 윈도우 캡처 원본을 기준으로 잘라 둥근 모서리 알파가 보존되도록 개선
- 자동 스냅 영역을 이동/리사이즈해도 선택이 원래 윈도우 안에 남아 있으면 윈도우 기반 이미지 캡처를 유지하고, 벗어나면 기존 영역 캡처로 처리

## 1.0.59
- 라이브러리 이미지 바깥 클릭이나 Esc로 도구가 해제될 때 상단 도구 메뉴의 선택 상태도 함께 해제되도록 수정

## 1.0.58
- 캡처 선택 HUD를 닫을 때 내용, 그림자, 알파를 먼저 제거해 화면 서버 잔상이 캡처에 남을 가능성을 줄임
- 이미지 캡처와 영상 시작 모두 HUD 닫힘이 반영된 뒤 실행되도록 지연 시간을 조정

## 1.0.57
- 창 영역 자동 스냅 후 이미지 캡처 시 배경이 섞인 사각형 crop 대신 윈도우 전용 캡처를 사용하도록 수정
- 자동 스냅된 전체 창은 둥근 모서리 바깥 배경이 캡처되지 않게 하고, 사용자가 크기/위치를 조정한 선택은 기존 영역 캡처를 유지

## 1.0.56
- 캡처 모드 선택 HUD가 이미지 캡처 결과에 함께 찍힐 수 있던 문제 수정
- 캡처 PNG 저장/복사 시 둥근 모서리 등 투명 영역의 알파가 채워지지 않도록 보존 처리 강화

## 1.0.55
- 텍스트 입력 완료 후 이미지 바깥/빈 영역 클릭 시 텍스트 도구가 해제되도록 수정
- 텍스트 커밋 중 포커스 종료 이벤트가 다시 들어와 같은 텍스트가 중복 생성될 수 있던 문제 보강
- 텍스트 입력 완료 클릭 직후에도 기존 오브제를 바로 선택하거나 드래그 이동할 수 있도록 흐름 정리

## 1.0.54
- 텍스트 입력 완료를 위해 빈 영역을 클릭한 직후 같은 클릭이 새 텍스트 입력까지 시작해 중복 텍스트가 생길 수 있던 문제 수정

## 1.0.53
- 라이브러리 편집 오브제 전체를 클릭해 선택하고 드래그로 이동할 수 있도록 개선
- 번호, 화살표, 사각형, 원, 모자이크도 선택 테두리로 현재 대상을 표시하도록 보강
- 선택된 편집 오브제를 Delete 또는 Backspace로 삭제할 수 있도록 추가

## 1.0.52
- 라이브러리 이미지 바깥 여백을 클릭하면 선택된 텍스트/말풍선과 현재 편집 도구가 해제되도록 개선
- 크롭 모드에서는 가장자리 핸들 보정은 유지하되, 핸들이 아닌 바깥 여백 클릭은 편집 취소로 처리

## 1.0.51
- 텍스트/말풍선을 선택 가능한 캔버스 객체처럼 다루도록 UX 개선
- 한 번 클릭으로 선택, 드래그로 이동, 더블클릭 또는 Return으로 텍스트 편집하도록 동작 정리
- 선택된 텍스트/말풍선에 점선 테두리와 핸들을 표시해 편집 대상을 명확히 구분
- 말풍선 박스 위치를 별도 상태로 저장해 입력 중/완료 후/이동 후 위치가 일관되도록 개선

## 1.0.50
- 말풍선 입력 중 실제 말풍선 배경과 꼬리 위에서 바로 입력되도록 개선
- 입력 중 텍스트 길이에 맞춰 말풍선 크기가 즉시 따라오도록 보강
- 도구 모드와 무관하게 기존 텍스트/말풍선을 클릭해 수정하거나 드래그해 위치를 조정할 수 있도록 개선

## 1.0.49
- 라이브러리 텍스트 주석을 클릭 후 드래그해 위치를 조정할 수 있도록 개선
- 텍스트 모드에서 빈 곳을 계속 클릭해 새 텍스트를 연속 입력할 수 있도록 수정
- 말풍선 주석의 본문 위치와 화살표 끝점을 각각 드래그로 조정할 수 있도록 개선
- 말풍선 생성 중 클릭 직후부터 미리보기가 보이도록 보강

## 1.0.48
- 라이브러리 이미지 편집기에 텍스트와 말풍선 주석 도구 추가
- 라이브러리에서 `Cmd+C`로 복사할 때 번호, 텍스트, 말풍선, 도형, 모자이크 등 편집 결과가 합성된 이미지로 복사되도록 보강

## 1.0.47
- 캡처 모드 진입 후 새 창에 처음 호버할 때도 접근성 트리 계산을 백그라운드에서 수행하도록 바꿔 일시 프리즈를 완화
- 정밀 콘텐츠 영역 계산이 끝나기 전에는 즉시 휴리스틱 영역을 보여주고, 계산 완료 후 조용히 정밀 영역으로 갱신

## 1.0.46
- 캡처 모드에서 같은 창 위를 이동할 때 접근성 트리를 반복 순회하지 않도록 콘텐츠 영역 계산 결과를 캐시해 호버 렉을 완화

## 1.0.45
- 브라우저 전용 가정 대신 `AXWebArea`/`AXScrollArea`/`AXTable`/`AXOutline`/`AXList` 등 범용 콘텐츠 컨테이너 후보를 스코어링해 커서 위치에 맞는 영역을 선택하도록 개선

## 1.0.44
- 브라우저 콘텐츠 영역 계산에서 접근성 창 매칭이 실패해도 focused/main window로 재시도하도록 보강
- 접근성 권한이 없거나 `AXWebArea`를 찾지 못할 때 쓰는 Chrome/Safari/Edge/Firefox 계열 fallback 상단 영역 값을 더 현실적인 브라우저 UI 높이에 맞게 조정

## 1.0.43
- 브라우저 콘텐츠 영역은 접근성 API의 실제 `AXWebArea`를 우선 사용해 고정 높이 추정보다 정확하게 계산

## 1.0.42
- 창 위에서 캡처 영역을 고를 때 커서 위치에 따라 상단 메뉴/툴바 영역이면 전체 창, 콘텐츠 영역이면 콘텐츠만 자동 선택하도록 개선
- 콘텐츠만 선택되는 경우 전체 창 경계는 점선으로, 실제 선택 영역은 강조선으로 표시해 어떤 영역이 캡처될지 더 명확하게 안내

## 1.0.41
- 조정 가능한 캡처 영역을 띄운 상태에서도 이미지 캡처/영상 촬영 선택 HUD가 함께 보이도록 복구
- HUD 버튼을 누르는 순간의 최신 8핸들 조정 영역으로 이미지 캡처 또는 영상 촬영을 시작하도록 수정

## 1.0.40
- 캡처 모드에서 창을 클릭해 선택해도 바로 캡처하지 않고, 해당 창 영역을 8개 핸들로 먼저 조정할 수 있도록 수정
- 선택 영역 바깥만 어둡게 덮어 실제 캡처될 영역이 더 선명하게 보이도록 오버레이 표시 개선

## 1.0.39
- 캡처 영역 선택 직후 바로 선택 HUD가 떠서 조정 없이 확정되는 것처럼 보이던 흐름 수정 — 먼저 8개 핸들로 영역을 조정하고 Enter 또는 더블클릭으로 확정한 뒤 이미지 캡처/영상 촬영을 선택

## 1.0.38
- 라이브러리 왼쪽 썸네일 목록에서 이미지/영상/GIF를 드래그앤드롭으로 다른 앱(Finder·메일·슬랙 등)에 바로 파일 첨부 가능

## 1.0.37
- 캡처 영역을 조정할 때 선택 HUD가 오버레이 뒤로 숨어버리던 문제 수정 — HUD가 항상 오버레이 위에 표시

## 1.0.36
- 캡처 영역을 핸들로 조절하거나 이동하는 동안 선택 HUD가 영역을 실시간으로 따라다니도록 개선 (기존에는 조절이 끝난 뒤에만 이동)

## 1.0.35
- 영역을 선택하면 조정 핸들과 함께 선택 HUD(이미지 캡처/영상 촬영/취소)가 바로 표시 — Enter 안내 문구 제거, HUD 버튼이 곧 캡처 확정
- HUD가 떠 있는 동안에도 핸들로 영역을 계속 수정할 수 있고, 버튼을 누르는 시점의 최종 영역으로 캡처/녹화

## 1.0.34
- 캡처 영역을 드래그한 뒤 바로 캡처하지 않고 조정 단계로 진입 — 점선 테두리와 8개 핸들로 영역을 정확히 확인·수정한 뒤 Enter 또는 더블클릭으로 캡처 확정
- 핸들 드래그 중 확대경을 띄워 픽셀 단위로 경계 조절 가능, 영역 내부 드래그로 위치 이동
- 조정 중에는 캡처되지 않는 주변을 더 어둡게 표시해 캡처 범위를 명확하게 구분

## 1.0.33
- 라이브러리 왼쪽 썸네일 목록에서 파일명과 확장자를 함께 표시해 이미지/영상/GIF 항목을 구분하기 쉽게 개선

## 1.0.32
- 영상 촬영 중 선택 영역 바깥을 어둡고 흐린 오버레이로 덮어 현재 녹화 범위를 쉽게 확인할 수 있도록 개선
- 라이브러리 영상 미리보기에서 Space 키로 재생/일시정지를 토글할 수 있도록 지원

## 1.0.31
- 캡처 선택 HUD에서 영상 촬영을 고를 수 있으므로 상태바 메뉴의 별도 `영상 촬영` 항목 제거

## 1.0.30
- 선택 HUD와 촬영 중 HUD의 버튼을 고대비 전용 버튼으로 바꿔 어두운 배경에서도 글자가 확실히 보이도록 개선

## 1.0.29
- 캡처 영역을 선택한 뒤 `이미지 캡처` 또는 `영상 촬영`을 고르는 선택 HUD 추가
- 선택 HUD가 실제 이미지 캡처나 영상 녹화에 섞이지 않도록 선택 후 닫힌 다음 작업을 시작하도록 처리

## 1.0.28
- 영상 편집에서 시작/끝 핸들을 드래그할 때 재생 화면도 해당 시점으로 이동해 선택 장면을 바로 확인할 수 있도록 개선

## 1.0.27
- 라이브러리에서 영상을 볼 때 macOS 기본 플레이어 HUD가 커스텀 편집 HUD 위에 뜨지 않도록 제거

## 1.0.26
- 영상 촬영 HUD를 고정 다크 배경으로 바꿔 글자와 버튼 대비를 개선
- 영상 편집의 MP4 내보내기 문구를 `선택 구간 MP4 저장`으로 바꿔 선택한 초 구간만 새 파일로 저장된다는 의미를 명확히 표시

## 1.0.25
- 영상 촬영 HUD가 다른 창이나 화면을 클릭해도 사라지지 않도록 패널 표시 방식을 보강
- 상태바 메뉴의 `촬영 일시정지` 항목을 실제 영상 촬영 중일 때만 보이도록 개선

## 1.0.24
- 영상 편집의 구간 잘라내기 HUD를 단일 타임라인과 시작/끝 핸들 기반으로 개선
- 라이브러리 영상 편집 창의 기본 크기를 키워 재생과 구간 편집 컨트롤이 눌리지 않도록 조정
- 업데이트 확인 창이 뒤에 숨지 않도록 앱을 활성화하고 Sparkle 업데이트 창을 앞으로 가져오도록 보강

## 1.0.23
- 영상 편집의 잘라내기를 화면 영역 크롭이 아니라 시작/끝 초 구간 잘라내기로 변경
- 현재 재생 위치를 구간 시작/끝으로 지정하는 버튼 추가
- GIF 내보내기도 선택한 초 구간 기준으로 30프레임/45프레임 무한 반복 GIF를 만들도록 변경

## 1.0.22
- 라이브러리에서 GIF 항목을 WebKit 기반 미리보기로 표시해 애니메이션이 재생되도록 수정
- 영상 편집의 잘라내기 흐름을 `영역 선택` → 드래그 → `선택 영역 잘라내기` 순서로 명확히 변경
- 영상 재생 컨트롤을 평소에는 가리지 않도록 잘라내기 오버레이가 영역 선택 모드에서만 마우스 입력을 받게 수정
- 선택 영역이 없을 때 MP4 잘라내기가 전체 영상으로 실행되지 않도록 방지

## 1.0.21
- 영상 촬영 HUD를 마우스로 끌어 원하는 위치로 옮길 수 있게 개선
- 녹화 HUD가 실제 녹화 파일에 잡히지 않도록 캡처 제외 설정 유지
- 녹화 종료 후 라이브러리에서 MP4를 바로 선택해 간단 편집 가능
- 영상 편집에서 지정 영역 MP4 잘라내기와 무한 반복 GIF 내보내기 지원
- GIF 내보내기에 30프레임, 45프레임 옵션 추가
- 모자이크 도구가 더 명확하게 가려지도록 블록 크기와 다운샘플 강도 조정

## 1.0.20
- 영상 촬영에서 창을 클릭하면 정지 캡처처럼 동작하던 문제 수정: 하이라이트된 창 영역도 영상 녹화로 시작

## 1.0.19
- 영상 촬영 중임을 바로 알 수 있도록 화면 하단에 녹화 HUD 추가
- 녹화 HUD와 메뉴바에서 일시정지/재개/중지 제어 지원

## 1.0.18
- 자동 업데이트 ZIP을 GitHub Release 다운로드 URL 대신 repo raw URL로 제공해 다운로드 실패 가능성 감소

## 1.0.17
- 자동 업데이트 ZIP 공개 다운로드 검증을 릴리스 절차에 추가

## 1.0.16
- 영상 촬영 추가 — 메뉴바에서 영역을 선택한 뒤 화면을 녹화하고, 촬영 중지 시 MP4 파일로 저장

## 1.0.15
- 캡처를 시작할 때 첫 클릭이 무시돼 두 번 클릭해야 하던 문제 수정 — 오버레이가 뜨면 첫 클릭/드래그부터 바로 선택됨

## 1.0.14
- 라이브러리 편집기에 모자이크 도구 추가 — 가리고 싶은 영역을 드래그하면 그 사각형을 모자이크(블록)로 처리. ⌘Z 되돌리기, 복사·저장 시 함께 반영

## 1.0.13
- 캡처 화면을 우클릭(또는 Esc)으로 언제든 취소할 수 있도록 개선
- 단축키 변경이 잘 안 되던 문제 수정 — 녹화 중에는 기존 단축키를 잠시 꺼서 정상적으로 새 조합을 등록
- 저장 폴더를 직접 지정하면 캡처가 그 폴더에 저장되도록 변경 (기본값은 바탕화면/oh-my-opensnap, '기본값' 버튼으로 복귀). 혼동되던 '파일로 저장' 옵션 제거

## 1.0.12
- 캡처 모드의 크로스헤어·화면 어두워짐 오버레이가 OBS 등 외부 화면 녹화에도 보이도록 수정 (앱 자체 캡처 결과에는 여전히 안 찍힘)

## 1.0.11
- 앱 아이콘(로고) 새 디자인으로 교체

## 1.0.10
- 크롭 꼭지점 핸들이 이미지 가장자리에 걸려 잘 안 눌리던 문제 수정 — 이미지 바깥 여백을 눌러도 가까운 핸들이 잡힘

## 1.0.9
- 크롭 범위 조정도 되돌리기(⌘Z) 대상에 포함 — 핸들로 바꾼 범위를 한 단계씩 되돌릴 수 있음
- 크롭의 가운데 핸들(점) 제거 (안쪽을 드래그하면 이동은 그대로)

## 1.0.8
- 크롭 시 핸들을 드래그하면 커서 주변을 확대해 보여주는 확대경(루페) 추가 — 정밀하게 자르기
- 미리보기 가장자리에 여백을 둬 크롭 꼭지점 핸들을 잡기 쉽게 개선

## 1.0.7
- 큰 캡처가 라이브러리 창에 맞게 제대로 축소되지 않던 문제 수정 (맞춤 배율 계산 안정화)
- 이미지 확대/축소 중 화면이 떨리던 현상 개선

## 1.0.6
- 라이브러리 창 크기는 사용자가 맞춘 그대로 유지하고, 캡처 크기에 따라 줌(확대/축소)을 조절해 항상 창에 일관되게 채워 보이도록 변경

## 1.0.5
- 캡처 후 이미지 크기에 맞춰 라이브러리 창 크기를 자동 조절 (너무 크거나 작을 때)
- 업데이트 알림 창에 변경 내용(릴리스 노트) 표시

## 1.0.4
- 자동 업데이트(Sparkle) 동작 검증용 릴리스

## 1.0.3
- 배포 서명을 자체서명 인증서로 변경 — 업데이트해도 화면 녹화 권한이 유지됨

## 1.0.2
- 자동 업데이트 테스트 릴리스

## 1.0.1
- Sparkle 자동 업데이트 도입
- 캡처 진입 시 크로스헤어가 좌상단(0,0)에 잠깐 보이던 문제 수정
